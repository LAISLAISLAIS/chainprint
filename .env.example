# Copy values into js/auth/config.js (anon key is public; protect data with RLS).

# Supabase → Project Settings → API
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=your_anon_key

# Optional later: Google / Apple via Supabase Auth providers
# GOOGLE_CLIENT_ID=
# APPLE_CLIENT_ID=
